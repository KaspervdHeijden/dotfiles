# Config .dotfiles example file

# Should we be forced to commit a private fork check?
# export DF_CHECK_FORK=0;

# Should we check for DOS line endings?
# export DF_CHECK_LINE_ENDINGS=0;

# Should we be allowed to commit in master?
# export DF_CHECK_MASTER=0;

# Defines how deep repositories are searched in relative to the directories given in $DF_REPO_DIRS below.
# export DF_CDS_MAX_DEPTH=2;

# List of directories used to search for repositories for the cds() function.
# export DF_CDS_REPO_DIRS=();

# Should repositories show dirty status in prompt? This may slow down the prompt.
# export DF_GIT_PROMPT_SHOW_DIRTY=0;

# Color of the host in the prompt for bash/zsh, defaults to green.
# export DF_HOST_PROMPT_COLOR_BASH=;
# export DF_HOST_PROMPT_COLOR_ZSH=;
